import { Header } from "../layouts/Common/Header/Header";

export const Home = () => {
  return (
    <div className="flex h-full w-full flex-col bg-theme-blue">
      <Header />
    </div>
  );
};

export default Home;
